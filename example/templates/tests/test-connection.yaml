apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jjchart.fullname" . }}-test-connection"
  labels:
    {{- include "jjchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jjchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
